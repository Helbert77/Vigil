{
  "mcpServers": {
    "Stripe": {
      "command": "npx",
      "args": [
        "-y",
        "@stripe/mcp",
        "--tools=all"
      ],
      "env": {
        "STRIPE_SECRET_KEY": "<SUBSTITUA_PELA_SUA_STRIPE_SECRET_KEY>"
      }
    }
  }
}

